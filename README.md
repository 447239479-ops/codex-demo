# codex-demo

本仓库根目录提供一个可离线使用的“中文取名神器”示例页面：

- 直接双击 `index.html`，或在浏览器地址栏输入 `file:///路径/index.html` 即可使用。
- 页面为单文件实现，不依赖任何构建工具或外部网络资源，可离线运行。
- 如需重新生成或查看源码，可打开 `index.html` 文件查阅详细注释与脚本实现。

> 若克隆或下载后未看到文件，请确认已切换到包含最新提交的分支（例如 `work`）。
